### Prerequisites

* Node.js v8 or above

You can check your node version using the command:

```
node --version
```
 

### Start a game
```
npm install
npm start    
```
now check on browser `http://localhost:3042/`